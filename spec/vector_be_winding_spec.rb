require "spec_helper"

describe VectorBeWinding do
  it "has a version number" do
    expect(VectorBeWinding::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
